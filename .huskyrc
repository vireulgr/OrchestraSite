{
  "hooks": {
    "pre-commit": "lint-staged",
    "post-checkout": "npm install && \
        . ./venv/bin/activate && \
        pip install -r requirements.txt \
    ",
    "post-merge": "npm install && \
        . ./venv/bin/activate && \
        pip install -r requirements.txt \
    "
  }
}
